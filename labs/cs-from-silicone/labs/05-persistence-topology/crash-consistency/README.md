# Crash Consistency

Learn what makes a completed write durable and recoverable after interruption.

Cover ordering, `fsync`, metadata versus data, journaling, write-ahead logging, and recovery.

Platform: macOS and Linux/Pi; use disposable filesystems or images for fault injection.
Prerequisite: filesystem layout.
