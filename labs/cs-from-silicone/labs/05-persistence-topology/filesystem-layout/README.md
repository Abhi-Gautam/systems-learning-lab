# Filesystem Layout

Learn how a filesystem maps names and file offsets to persistent blocks.

Cover directories, metadata, inodes or equivalents, block allocation, caching, and locality.

Platform: macOS and Linux/Pi; study observed behavior without modifying mounted filesystem internals.
Prerequisite: file API.
