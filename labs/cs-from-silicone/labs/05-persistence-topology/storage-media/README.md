# Storage Media

Learn why disks, SSDs, and RAID configurations have different latency, bandwidth, and failure behavior.

Cover seek/rotation, flash erase blocks, write amplification, parallelism, redundancy, and rebuild cost.

Platform: macOS and Linux/Pi; use measurements and models, never destructive device tests.
Prerequisite: I/O devices and filesystem layout.
