# File API

Learn the kernel interface that turns names into open file descriptions and byte streams.

Cover descriptors, offsets, `open`/`read`/`write`/`close`, permissions, buffering, and atomicity.

Platform: macOS and Linux/Pi.
Prerequisite: syscalls and I/O devices.
