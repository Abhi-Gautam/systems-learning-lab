# 05 — Persistence and Topology

Learn how bytes travel to persistent media and how hardware placement changes their cost.

Order: devices → file API → filesystem layout → crash consistency → storage media → NUMA.

Platform: macOS and Linux/Pi; NUMA is deferred until suitable hardware exists.
