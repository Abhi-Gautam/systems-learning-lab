# Linking and Loading

Learn how separately compiled object files become a runnable process.

Cover symbols, relocations, static versus dynamic libraries, symbol lookup, and the dynamic loader.

Platform: macOS; compare Mach-O behavior with Linux on the Pi when useful.
Prerequisite: assembly and function calls.
