# Data Representation

Learn how one byte sequence is interpreted as integers, floating-point values, text, or structure fields.

Cover signedness, overflow, endianness, IEEE 754, alignment, padding, and byte-order conversion.

Platform: macOS.
Prerequisite: bits, bytes, and words.
