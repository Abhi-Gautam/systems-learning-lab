# 18 — LC 208 Implement Trie

LeetCode 208. Implement `insert(word)`, `search(word)`, and `startsWith(prefix)`.
Use `Node* child[26]` and an end marker. Implement recursive or iterative cleanup.
No map/string containers; accept `const char*` for this lab and write a small
character loop. State input alphabet and reject/define non-lowercase characters.

## Run

```bash
make test-18-lc208-trie
make test-18-lc208-trie ASAN=1
```
