# Memory Allocation

Learn how allocators turn virtual-memory regions into object-sized allocations.

Cover `malloc`/`free`, size classes, free lists, fragmentation, `brk` versus `mmap`, and lifetime errors.

Platform: macOS and Linux/Pi.
Prerequisite: address spaces.
