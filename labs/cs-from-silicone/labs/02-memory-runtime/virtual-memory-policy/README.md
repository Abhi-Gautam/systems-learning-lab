# Virtual Memory Policy

Learn why memory is paged on demand and what happens when RAM pressure exceeds the working set.

Cover demand paging, reclaim, eviction, swap, page-fault cost, and locality.

Platform: macOS and Linux/Pi; avoid destructive pressure experiments.
Prerequisite: address translation.
