# Address Translation

Learn how a virtual address becomes a physical memory access.

Cover page-table entries, page-table walks, TLB hits/misses, protection bits, and page faults.

Platform: macOS and Linux/Pi; use observation rather than privileged page-table access.
Prerequisite: address spaces and assembly addressing.
