# Address Spaces

Learn the layout and isolation of a process's virtual address space.

Cover stack, heap, mapped regions, permissions, ASLR, guard pages, and copy-on-write.

Platform: macOS with `vmmap`; Linux/Pi with `/proc/<pid>/maps`.
Prerequisite: pointers and processes.
