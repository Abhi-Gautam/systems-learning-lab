# Cache Coherence

Learn why cores need a protocol before they can safely cache the same memory line.

Cover cache lines, ownership, invalidation, false sharing, ping-pong traffic, and measurement pitfalls.

Platform: macOS and Linux/Pi; observe effects without assuming a specific vendor protocol.
Prerequisite: threads and cache-locality basics.
