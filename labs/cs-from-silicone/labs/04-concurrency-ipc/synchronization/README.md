# Synchronization

Learn the primitives that make shared-state updates safe and waiting efficient.

Cover mutexes, condition variables, semaphores, lock contention, and the Linux futex slow path.

Platform: macOS for portable primitives; Linux/Pi for `futex`.
Prerequisite: cache coherence and memory ordering.
