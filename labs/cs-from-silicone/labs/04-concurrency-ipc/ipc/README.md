# IPC

Learn the kernel-mediated channels used by isolated processes.

Order: pipes → signals → Unix sockets → message queues; compare copies, blocking, boundaries, and payloads.

Platform: macOS and Linux/Pi; mqueue availability is verified before implementation.
Prerequisite: process lifecycle and file descriptors.
