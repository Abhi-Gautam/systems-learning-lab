# Syscalls and Traps

Learn the controlled transition from unprivileged user code to kernel code.

Cover syscall ABI, trap entry/return, errno, file descriptors, and syscall tracing.

Platform: macOS with `dtruss` where permitted; Linux/Pi with `strace`.
Prerequisite: assembly and function calls.
