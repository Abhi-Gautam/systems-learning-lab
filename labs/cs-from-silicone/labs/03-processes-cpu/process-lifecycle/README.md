# Process Lifecycle

Learn how a parent creates, replaces, observes, and reaps child processes.

Cover `fork`, `exec`, `wait`, exit status, zombies, orphaning, and copy-on-write.

Platform: macOS and Linux/Pi.
Prerequisite: syscalls and address spaces.
